import { auth } from "@/auth"; import { redirect } from "next/navigation"; import { prisma } from "@/lib/prisma"; import { AdminNav } from "@/components/admin-nav";
const labels:Record<string,string>={APPROVE:"Approved",DECLINE:"Declined",SUSPEND:"Suspended",REINSTATE:"Reinstated",COURSE_CHANGE:"Changed course for",DELETE:"Deleted"};
export default async function Activity(){if((await auth())?.user.role!=="admin")redirect("/admin/login");const activity=await prisma.activityLog.findMany({orderBy:{createdAt:"desc"}});return <main className="admin"><AdminNav/><header><p className="eyebrow">ADMINISTRATION</p><h1>Activity log</h1><p>Recent student account actions.</p></header><div className="table activity-table">{activity.length===0?<section className="empty"><h2>No activity yet</h2><p>Student access changes will appear here.</p></section>:activity.map(entry=><article key={entry.id}><div><h2>{entry.adminEmail}</h2><p>{labels[entry.action]} <strong>{entry.studentName}</strong> ({entry.studentEmail})</p></div><time>{entry.createdAt.toLocaleString()}</time></article>)}</div></main>}
