"use server";
import { auth } from "@/auth"; import { prisma } from "@/lib/prisma"; import { revalidatePath } from "next/cache"; import { ApprovalStatus, Course } from "@prisma/client"; import bcrypt from "bcryptjs"; import { redirect } from "next/navigation";
async function requireAdmin(){if((await auth())?.user.role!=="admin")throw new Error("Unauthorized");}
export async function setApproval(id:string,status:ApprovalStatus){await requireAdmin();await prisma.student.update({where:{id},data:{approvalStatus:status}});revalidatePath("/admin");revalidatePath("/admin/students");}
export async function updateCourse(id:string,formData:FormData){await requireAdmin();const course=formData.get("course");if(!Object.values(Course).includes(course as Course))throw new Error("Invalid course");await prisma.student.update({where:{id},data:{selectedCourse:course as Course}});revalidatePath("/admin/students");}
export async function createAdmin(formData:FormData){await requireAdmin();const email=String(formData.get("email")||"").trim().toLowerCase();const password=String(formData.get("password")||"");if(!/^\S+@\S+\.\S+$/.test(email)||password.length<8)redirect("/admin/admins?error=invalid");if(await prisma.admin.findUnique({where:{email}}))redirect("/admin/admins?error=duplicate");await prisma.admin.create({data:{email,passwordHash:await bcrypt.hash(password,12)}});revalidatePath("/admin/admins");redirect("/admin/admins?created=1");}
