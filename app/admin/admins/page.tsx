import { auth } from "@/auth"; import { redirect } from "next/navigation"; import { prisma } from "@/lib/prisma"; import { AdminNav } from "@/components/admin-nav"; import { createAdmin } from "../actions";
export default async function Admins({searchParams}:{searchParams:Promise<{error?:string;created?:string}>}){if((await auth())?.user.role!=="admin")redirect("/admin/login");const [admins,params]=await Promise.all([prisma.admin.findMany({orderBy:{createdAt:"asc"}}),searchParams]);return <main className="admin"><AdminNav/><header><p className="eyebrow">ADMINISTRATION</p><h1>Manage admins</h1><p>Create credentials for another StepUp administrator.</p></header><section className="card admin-card"><h2>New admin account</h2>{params.error&&<p className="error">{params.error==="duplicate"?"An admin with that email already exists.":"Enter a valid email and a password of at least 8 characters."}</p>}{params.created&&<p className="success">Admin account created.</p>}<form action={createAdmin} className="form compact"><input name="email" type="email" placeholder="Admin email" required/><input name="password" type="password" minLength={8} placeholder="Password (8+ characters)" required/><button>Create admin</button></form></section><div className="table">{admins.map(a=><article key={a.id}><div><h2>{a.email}</h2><p>Created {a.createdAt.toLocaleDateString()}</p></div></article>)}</div></main>}
