import { auth } from "@/auth"; import { redirect } from "next/navigation"; import { prisma } from "@/lib/prisma"; import { setApproval } from "./actions";
const label:Record<string,string>={AFTER_SEE:"After SEE",CLASS_11:"Class 11",CLASS_12:"Class 12"};
export default async function Admin(){const session=await auth();if(session?.user.role!=="admin")redirect("/admin/login");const students=await prisma.student.findMany({where:{approvalStatus:"PENDING"},orderBy:{createdAt:"asc"}});return <main className="admin"><header><p className="eyebrow">STEPUP ACADEMY · ADMIN</p><h1>Pending registrations</h1><p>Review student access requests.</p></header>{students.length===0?<section className="empty"><h2>All caught up</h2><p>There are no pending registrations.</p></section>:<div className="table">{students.map(s=><article key={s.id}><div><h2>{s.name}</h2><p>{s.email} · {label[s.selectedCourse]} · {s.createdAt.toLocaleDateString()}</p></div><div className="row"><form action={setApproval.bind(null,s.id,"APPROVED")}><button>Approve</button></form><form action={setApproval.bind(null,s.id,"DECLINED")}><button className="decline">Decline</button></form></div></article>)}</div>}</main>}
