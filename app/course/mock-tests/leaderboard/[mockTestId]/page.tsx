import Link from "next/link";
import { auth } from "@/auth";
import { prisma } from "@/lib/prisma";
import { redirect } from "next/navigation";
import { Leaderboard } from "@/components/leaderboard";

export default async function TestLeaderboard({ params }: { params: Promise<{ mockTestId: string }> }) { const session = await auth(); if (session?.user.role !== "student" || !session.user.id) redirect("/login?error=signed-in-elsewhere"); const { mockTestId } = await params; const test = await prisma.mockTest.findFirst({ where: { id: mockTestId, course: { students: { some: { id: session.user.id } } } }, select: { name: true, leaderboardHidden: true } }); if (!test || test.leaderboardHidden) redirect("/course"); const attempts = await prisma.attempt.findMany({ where: { mockTestId, status: "COMPLETED" }, include: { student: { select: { id: true, name: true } } } }); const best = new Map<string, { name: string; score: number; max: number }>(); for (const attempt of attempts) { const current = best.get(attempt.studentId); const percent = attempt.maxPossibleScore ? (attempt.score || 0) / attempt.maxPossibleScore : 0; if (!current || percent > current.score / current.max) best.set(attempt.studentId, { name: attempt.student.name, score: attempt.score || 0, max: attempt.maxPossibleScore || 0 }); } const ranked = [...best.entries()].sort((a, b) => b[1].score / b[1].max - a[1].score / a[1].max).map(([studentId, item], index) => ({ rank: index + 1, name: item.name, value: `${item.score}/${item.max}`, isYou: studentId === session.user.id })); const own = ranked.find((row) => row.isYou); return <main className="course"><p className="eyebrow">MOCK TEST LEADERBOARD</p><h1>{test.name}</h1><Leaderboard rows={ranked.slice(0, 10)} yourRank={own ? { rank: own.rank, value: own.value } : undefined} /><Link className="button secondary" href="/course">Back to course</Link></main>; }
