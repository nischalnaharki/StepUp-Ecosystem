import { auth } from "@/auth";
import { prisma } from "@/lib/prisma";
import { redirect } from "next/navigation";
import Link from "next/link";
export default async function Course(){const session=await auth();if(!session)redirect("/login");if(session.user.role!=="student")redirect("/admin");const student=await prisma.student.findUnique({where:{id:session.user.id}});if(!student)redirect("/login");if(student.approvalStatus==="PENDING")return <Gate title="Registration pending" text="Your registration is awaiting admin approval. Please check back soon."/>;if(student.approvalStatus==="DECLINED")return <Gate title="Registration declined" text="Your registration was not approved. Contact StepUp Academy if you believe this is a mistake."/>;if(student.approvalStatus==="SUSPENDED")return <Gate title="Access suspended" text="Your access has been suspended. Contact StepUp Academy support for help."/>;if(student.selectedCourse!=="AFTER_SEE")return <main className="course"><p className="eyebrow">STEPUP ACADEMY</p><h1>{student.selectedCourse==="CLASS_11"?"Class 11":"Class 12"}</h1><section className="empty"><h2>Coming soon</h2><p>We&apos;re preparing this course for you. Stay tuned!</p></section></main>;return <main className="course"><p className="eyebrow">AFTER SEE · YOUR BRIDGE TO +2</p><h1>Welcome, {student.name.split(" ")[0]}!</h1><p className="lead">Start strong. Your essentials are right here.</p><div className="course-grid"><section className="course-card"><span>01</span><h2>Book</h2><p>Your After SEE study book is available here once it has been published.</p><a className="button" href="/after-see-book.pdf" target="_blank">Open book PDF</a></section><section className="course-card"><span>02</span><h2>Mock Test</h2><p>Put your preparation to the test.</p><div className="soon">Coming soon</div></section></div></main>}
function Gate({title,text}:{title:string;text:string}){return <main className="auth-page"><section className="card centered"><h1>{title}</h1><p>{text}</p><Link className="button" href="/">Back home</Link></section></main>}
